{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cargo build --release --bin sync-mail"
  },
  "deploy": {
    "cronSchedule": "0 5 * * *",
    "startCommand": "./target/release/sync-mail run",
    "restartPolicyType": "NEVER"
  }
}