# Application Database URL (PostgreSQL)
SYNC__APP_DB_URL=postgresql://user:password@host:port/database

# DDB Database URL (MySQL)
SYNC__DDB_URL=mysql://user:password@host:port/database

# Mail Database URL (PostgreSQL)
SYNC__MAIL_DB_URL=postgresql://user:password@host:port/database

# Optional: Logging configuration
# SYNC__LOG=sync_server=info,mailchimp=info,db=info
